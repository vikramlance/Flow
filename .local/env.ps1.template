# env.ps1.template — Machine-Local Environment Variables
# -------------------------------------------------------
# Copy this file to .local\env.ps1 and fill in your values.
# Source it before running any .specify scripts or quickstart commands:
#   . .\.local\env.ps1
#
# THIS FILE IS A TEMPLATE — it contains no real paths.
# The real .local\env.ps1 is gitignored and stays on your machine only.

# ── Android SDK ──────────────────────────────────────────────────────────────
# Set to the root of your Android SDK installation.
# Example (Windows): $env:ANDROID_HOME = "C:\Users\<your-name>\AppData\Local\Android\Sdk"
# Example (macOS):   $env:ANDROID_HOME = "/Users/<your-name>/Library/Android/sdk"
$env:ANDROID_HOME = "FILL_IN_YOUR_SDK_PATH_HERE"

# ── Java (optional — usually provided by Android Studio) ─────────────────────
# Only needed if you run Gradle outside Android Studio.
# $env:JAVA_HOME = "FILL_IN_YOUR_JDK_PATH_HERE"

# ── Convenience: add platform-tools to session PATH ──────────────────────────
if ($env:ANDROID_HOME -and (Test-Path "$env:ANDROID_HOME\platform-tools")) {
    $env:PATH = "$env:ANDROID_HOME\platform-tools;$env:PATH"
}
